#include <bits/stdc++.h>
using namespace std;
int main()
{
    // map<int, int> m;
    // m.insert(pair<int, int>(1, 2));

    // map<int, int>::iterator i;
    // i = m.begin();

    // m[0]++;
    // cout << i->first << endl;
    // cout << i->second;

    string a = "yoyo";

    a.append(1, 't');
    cout << a;
}