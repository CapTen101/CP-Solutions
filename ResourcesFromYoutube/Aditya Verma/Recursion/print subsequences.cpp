#include <bits/stdc++.h>
using namespace std;

#define start \
    int t;    \
    cin >> t; \
    while (t--)

/*
sub string: it's always the continous set of characters in the string 

sub sequence: it can be made by picking any character from the string from anywhere ... free style string
BUT! order matters in subsequence. we cannot join a string, which is coming before a string, after it

sub set: set of all possible substrings of a bigger string
*/

void print()
{
}

int main()
{
    ios_base::sync_with_stdio(false);
}